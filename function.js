function sum (a, b) {
    return a + b;
}

const sumArrow = (a, b) => {
    return a + b;
}

// Mesma coisa que acima, mas com sintaxe mais curta
const sumArrow2 = (a, b) => a + b;

const sumValue = sum(2, 2);

console.log(sumValue);
